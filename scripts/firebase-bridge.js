// /scripts/firebase-bridge.js â€“ v9007// Single place to initialize Firebase (uses global window.firebaseConfig from firebase-config.js)// and to re-export the SDK pieces your app imports elsewhere.if (!window.firebaseConfig || typeof window.firebaseConfig !== 'object') {  throw new Error("[firebase-bridge] Missing global firebaseConfig. Make sure /scripts/firebase-config.js is loaded before this file.");}// Import the modular SDK directly from gstatic (works with your CSP)import { initializeApp } from "https://www.gstatic.com/firebasejs/11.0.1/firebase-app.js";import {  getAuth,  onAuthStateChanged,  signInWithEmailAndPassword,  createUserWithEmailAndPassword,  signOut,  sendPasswordResetEmail,  getIdTokenResult} from "https://www.gstatic.com/firebasejs/11.0.1/firebase-auth.js";import {  getFirestore,  doc,  getDoc,  setDoc,  updateDoc,  addDoc,  collection,  getDocs,  query,  where,  orderBy,  limit,  serverTimestamp} from "https://www.gstatic.com/firebasejs/11.0.1/firebase-firestore.js";import {  getStorage,  ref,  getDownloadURL,  uploadBytes} from "https://www.gstatic.com/firebasejs/11.0.1/firebase-storage.js";// Initializeexport const firebaseApp = initializeApp(window.firebaseConfig);// Instancesexport const auth = getAuth(firebaseApp);export const db = getFirestore(firebaseApp);export const storage = getStorage(firebaseApp);// Re-export AUTH APIexport {  onAuthStateChanged,  signInWithEmailAndPassword,  createUserWithEmailAndPassword,  signOut,  sendPasswordResetEmail,  getIdTokenResult};// Re-export FIRESTORE APIexport {  doc,  getDoc,  setDoc,  updateDoc,  addDoc,  collection,  getDocs,  query,  where,  orderBy,  limit,  serverTimestamp};// Re-export STORAGE helpersexport { ref, getDownloadURL, uploadBytes };